Major changes in GLProgram.

ibo.vala

                glBindBuffer (GL_ELEMENT_ARRAY_BUFFER, id);
-               glBufferData (GL_ELEMENT_ARRAY_BUFFER, data.length * sizeof (GLushort), (GLvoid[]) data, GL_STATIC_DRAW);
-               glBindBuffer (GL_ELEMENT_ARRAY_BUFFER, 0);
+               glBufferData (GL_ELEMENT_ARRAY_BUFFER, data.length * sizeof(GLushort), (GLvoid[])data, GL_STATIC_DRAW);
+               // 変更 - 使用しない ( unused )
+               //glBindBuffer (GL_ELEMENT_ARRAY_BUFFER, 0);
        }

japanese don't use Utils.vala

can't queue render:
(sesh:13050): Gtk-CRITICAL **: gtk_gl_area_queue_render: assertion 'GTK_IS_GL_AREA (area)' failed
sesh: Couldn't find current GLX or EGL context.

wrong context or not set up for epoxy???

actions 24 edit.svg for edit button for now.

pactl list | grep -C 10 noise
https://outflux.net/blog/archives/2009/04/19/recording-from-pulseaudio/


basically everything is stolen at this point.

might be a good idea to fix that before you publish anything.
